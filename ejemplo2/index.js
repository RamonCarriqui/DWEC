// Cero, undefined y false son lo mismo
var mivariable = "JOSE";
if (mivariable){

    console.log(`Variable contains ${mivariable}`);
}

// === Sirve para comprobar que son del mismo tipo y valen lo mismo

